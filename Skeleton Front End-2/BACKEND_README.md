# Backend Implementation Guide (Archived)

This document is kept for historical context from an earlier implementation phase.
It is **not** the source of truth for the current backend shape.

## Use these docs instead

- Root onboarding and runbook: `/README.md`
- Current Supabase setup and deploy flow: `/docs/SUPABASE_SETUP.md`
- Current code in repository:
  - `/supabase/functions/`
  - `/supabase/migrations/`

## Why this file is archived

The original content in this file referenced older paths, migration names, and phased plans
that no longer match the current `main` branch.

If you are setting up or modifying backend behavior, follow the docs listed above and verify
against the actual code under `/supabase`.
