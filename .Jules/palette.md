## 2024-05-15 - Form Accessibility Improvements\n**Learning:** React form inputs should explicitly link `htmlFor` to `id` for screen readers, and complex grouped inputs like "Date of Birth" require `<fieldset>` and `<legend>` with visually hidden labels for the individual inputs.\n**Action:** Update `PatientForm.tsx` to properly associate labels with inputs using `htmlFor` and `id`, and wrap the Date of Birth fields in a fieldset.
